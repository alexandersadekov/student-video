# Аудит переноса — 18 сентября 2026

Проверен GitHub commit 5301f996acd1f4e732da151d9b196b615ec2d53b в отдельном чистом клоне.

Перенесены правила, профиль Inter, локальные шрифты с OFL, примеры изображений, импорт, экспорт, горячие клавиши и регистрация композиций. Личные исходники, рендеры, модели и generated-композиции намеренно исключены. Это перенос шаблона, не резервная копия монтажей.

Исправлено:
- Scene-Carousel обрывался на кадре300 до финального morph; длительность507.
- Scene-Prompts обрывался до результата /metaads; длительность425.
- Tahoma в трёх шаблонных сценах заменён на Inter, описание в README согласовано.
- Служебные подписи в PromptExamples убраны; шрифтовая стрелка заменена SVG. Точное совпадение окна с актуальным ChatGPT этим не гарантируется.
- MontageStory: objectFit перенесён в поддерживаемый prop, убрана привязка начала переданного видео к кадру275.
- apply-research: запрещены пути вместо id/имени ассета; checked обязателен и проверяется как дата, не подставляется автоматически.
- В AGENTS закреплены выбор дубля, план действий каждой фразы и проверка остановок/яркости.

Проверено: npm ci с локальным cache, генерация пустых реестров, TypeScript, ESLint без предупреждений, 3 теста импорта, 10 проверок клавиш, 2 теста валидации research, bundle через API Remotion.
Обычный npm run build в этой среде ограничен чтением родительского каталога при загрузке remotion.config.ts. Сборка через API прошла; это не подтверждает запуск SETUP.cmd на другом ПК. Полный рендер и слуховая проверка в этом аудите не выполнялись.

Остаётся: смысловой выбор дублей и проверка динамики требуют агентского прохода. Сцены являются примерами, а не автоматической режиссурой нового ролика. Для передачи ученику нужен отдельный прогон SETUP/import/export на чистом Windows.
